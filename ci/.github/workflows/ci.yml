name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  security-events: write
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAMESPACE: ${{ github.repository_owner }}

jobs:

  # test:
  #   name: Unit Tests
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"

  #     - name: Install Dependencies
  #       run: |
  #         pip install pytest ruff bandit

  #     - name: Ruff Lint
  #       run: |
  #         ruff check src || true

  #     - name: Bandit Security Scan
  #       run: |
  #         bandit -r src -lll -iii || true

  #     - name: Run Tests
  #       run: |
  #         pytest -q || true

  # sonar:
  #   name: SonarQube SAST
  #   runs-on: ubuntu-latest
  #   needs: test

  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - name: SonarQube Scan
  #       uses: SonarSource/sonarqube-scan-action@v8
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # gitleaks:
  #   name: Secret Scanning
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - uses: gitleaks/gitleaks-action@v2
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # build:
  #   name: Build & Scan Images
  #   runs-on: ubuntu-latest

  #   needs:
  #     - sonar
  #     - gitleaks

  #   strategy:
  #     fail-fast: false

  #     matrix:
  #       svc:
  #         - auth-service
  #         - account-service
  #         - transaction-service
  #         - notification-service
  #         - api-gateway

  #   steps:
  #     - uses: actions/checkout@v4

  #     - uses: docker/setup-buildx-action@v3

  #     - uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Build and Push Image
  #       id: build
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         file: docker/${{ matrix.svc }}.Dockerfile
  #         push: true
  #         tags: ghcr.io/${{ github.repository_owner }}/${{ matrix.svc }}:${{ github.sha }}

  #     - name: Trivy Scan
  #       uses: aquasecurity/trivy-action@master 
  #       with:
  #         image-ref: ghcr.io/${{ github.repository_owner }}/${{ matrix.svc }}:${{ github.sha }}
  #         format: sarif
  #         output: trivy-results.sarif
  #         severity: HIGH,CRITICAL

  #     - name: Upload Trivy Results
  #       uses: github/codeql-action/upload-sarif@v3
  #       with:
  #         sarif_file: trivy-results.sarif


    zap:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v4

        - name: Start Stack
          run: docker compose -f docker/docker-compose.yml up -d

        - name: Wait for Application
          run: sleep 30

        - name: Run ZAP Baseline with Docker
          run: |
            docker run --rm --network=host ghcr.io/zaproxy/zaproxy:stable \
              zap-baseline.py -t http://localhost:8443 -I
          

      